---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<h3 class={`text-xs font-semibold uppercase tracking-widest text-white/80 ${className}`}>
  <slot />
</h3>