---
import { site } from "../../data/site";
import { faqs } from "../../data/faq";
import Section from "./Section.astro";
---

<Section
  id="faq"
  ariaLabel="Frequently Asked Questions"
  class="bg-[hsl(var(--color-bg-warm-half))]"
  containerClass="relative"
>
  <div class="mx-auto max-w-7xl px-6 py-20 lg:py-28">
    <div class="text-center mb-16">
      <span class="inline-flex items-center px-4 py-1.5 rounded-full bg-[hsl(var(--color-primary))]/10 border border-[hsl(var(--color-primary))]/20 text-sm font-bold uppercase tracking-widest text-[hsl(var(--color-primary))] mb-3 block">
        FAQ
      </span>
      <h2 class="text-3xl font-black tracking-tight text-[hsl(var(--color-foreground))] sm:text-4xl">
        Questions, answered
      </h2>
    </div>

    <div class="mx-auto max-w-3xl">
      {faqs.map((faq, index) => (
        <div class="faq-item border-b border-[hsl(var(--color-border))]">
          <button
            class="faq-button w-full flex items-center justify-between py-5 text-left transition-colors hover:bg-[hsl(var(--color-primary)/0.05)] px-4 rounded-lg"
            data-index={index}
          >
            <span class="text-lg font-semibold text-[hsl(var(--color-foreground))] pr-4">{faq.question}</span>
            <svg class:list={["faq-chevron h-5 w-5 shrink-0 text-[hsl(var(--color-primary))] transition-transform duration-300", { "rotate-90": index === 0 }]} viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="9 18 15 12 9 6" />
            </svg>
          </button>
          <div class="faq-answer overflow-hidden transition-all duration-300" style={index === 0 ? "max-height: 500px;" : "max-height: 0;"}>
            <p class="px-4 pb-5 text-[hsl(var(--color-muted-foreground))] leading-relaxed">
              {faq.answer}
            </p>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 text-center">
      <p class="text-lg text-[hsl(var(--color-muted-foreground))]">
        Something else?&nbsp;
        <a href={site.demoUrl} class="text-[hsl(var(--color-primary))] font-semibold hover:underline">
          Ask us in a demo
        </a>
        .&nbsp;Prefer email?&nbsp;
        <a href={`mailto:${site.contact.email}`} class="text-[hsl(var(--color-primary))] font-semibold hover:underline">
          We'd love to hear from you
        </a>
      </p>
    </div>
  </div>
</Section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const buttons = document.querySelectorAll(".faq-button");

    buttons.forEach((button) => {
      button.addEventListener("click", () => {
        const index = button.getAttribute("data-index");
        const answer = button.parentElement?.querySelector(".faq-answer") as HTMLElement;
        const chevron = button.querySelector(".faq-chevron") as HTMLElement;
        const isOpen = answer.style.maxHeight !== "0px" && answer.style.maxHeight !== "";

        // Close all other answers
        document.querySelectorAll(".faq-answer").forEach((el, i) => {
          if (i !== Number(index)) {
            (el as HTMLElement).style.maxHeight = "0px";
            document.querySelectorAll(".faq-chevron")[i].style.transform = "rotate(0deg)";
          }
        });

        // Toggle current answer
        if (isOpen) {
          answer.style.maxHeight = "0px";
          chevron.style.transform = "rotate(0deg)";
        } else {
          answer.style.maxHeight = answer.scrollHeight + "px";
          chevron.style.transform = "rotate(90deg)";
        }
      });
    });
  });
</script>
