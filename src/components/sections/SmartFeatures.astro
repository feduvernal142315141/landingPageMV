---
import { smartFeatures } from "../../data/smartFeatures";
import { icons } from "../../data/icons";
---

<section id="smart-features" aria-label="Smart Features" class="relative overflow-hidden bg-[hsl(var(--color-bg-warm-half))] bg-grid-pattern">
  <div class="bg-[hsl(var(--color-bg-warm-half))] py-16 sm:py-20">
    <div class="scroll-reveal slide-up mx-auto max-w-7xl px-6">
      <span class="inline-flex items-center px-4 py-1.5 rounded-full bg-[hsl(var(--color-primary))]/10 border border-[hsl(var(--color-primary))]/20 text-sm font-bold uppercase tracking-widest text-[hsl(var(--color-primary))] mb-3 block">
        Features
      </span>
      <h2 class="mt-3 text-3xl font-black tracking-tight sm:text-4xl text-[hsl(var(--color-primary))]">
        Smart Features To Elevate Your Care
      </h2>
      <p class="mt-4 text-xl text-[hsl(var(--color-foreground))] font-normal tracking-wide">
        The operating system for <span class="font-bold">modern ABA practices</span>
      </p>
    </div>
  </div>

  <div class="mx-auto max-w-7xl px-6 pt-8 pb-20 lg:pt-10 lg:pb-28">
    <div class="grid grid-cols-1 gap-12 md:grid-cols-2">
      {smartFeatures.map((feature, i) => (
        <div class:list={[
          "scroll-reveal slide-up relative rounded-2xl border border-white/20 bg-[hsl(var(--color-bg-warm))] p-6 sm:p-8",
          i === 2 && "md:col-span-2 md:mx-auto md:w-1/2"
        ]}>
          <svg class="absolute left-5 top-5 h-7 w-7 text-[hsl(var(--color-primary))]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" set:html={icons[feature.icon]} />
          <span class="absolute right-5 top-5 text-xl font-black text-[hsl(var(--color-primary))]">{feature.number}</span>
          <div class="flex flex-col items-center text-center justify-between h-full">
            <h3 class="text-lg font-black tracking-tight sm:text-xl text-[#0f172a]" set:html={feature.title} />
            <p class="mt-3 text-lg text-[hsl(var(--color-foreground))] leading-relaxed text-left">
              {feature.description}
            </p>
            <ul class="mt-6 grid w-full grid-cols-1 gap-2 sm:grid-cols-2 flex-grow">
              {feature.bullets.map((bullet) => (
                <li class="flex items-start gap-2 rounded-lg bg-[#0f172a] p-3 text-left">
                  <svg class="mt-0.5 h-4 w-4 shrink-0 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" set:html={icons[bullet.icon]} />
                  <span class="text-sm font-bold text-white">{bullet.text}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
